spring:
  sql:
    init:
      mode: never
  jpa:
    defer-datasource-initialization: false

firebase:
  credentials-path: dummy-for-test

obrit:
  admin:
    username: admin
    password: admin

ocr:
  client:
    api-key: test
    url: http://localhost
    auth-header: x-test
    prompt: test
